# L.Y.V.R.A. Native Runtime

This directory establishes the native authority surface for the **L.Y.V.R.A. Plugin / Custom GPT** project.

## Start here

1. Read `CURRENT_POINTER.json`.
2. Read `AUTHORITY_CONTRACT.md`.
3. Read `CODEFORGE_STEWARDSHIP.md`.
4. Resolve the current repository HEAD on branch `lyvra`.
5. Treat repository state as the only productive truth for this Plugin / Custom GPT project.
6. Use Google Drive only as LYVRA-internal development/reference/provenance input when deliberately needed.

## Core distinction

**LYVRA is the identity and decision authority.  
GitHub is the Plugin / Custom GPT technical source of truth.  
CodeForge is the code-engineering and repository-stewardship capability.**

The repository may evolve. Current verified repository state always supersedes older product-state descriptions.
